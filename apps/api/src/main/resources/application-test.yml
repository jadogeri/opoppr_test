spring:
  datasource:
    url: jdbc:h2:mem:opoppr;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 1025
  docker:
    compose:
      enabled: false

opoppr:
  cors:
    allowed-origins: http://localhost:3000