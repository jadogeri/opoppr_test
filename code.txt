/**
 * LAT5 Sheet extender reference.
 *
 * The runtime implementation is loaded by the five LAT5 pages from
 * resources/js/lat5-sheet.js. Keep context-menu actions and validation there,
 * rather than duplicating them in Section1.java through Section5.java.
 *
 * Extenders used by the pages:
 *   inventorySheetExtender  - fixed Section 1 inventory rows
 *   filingSheetExtender     - Sections 2 and 3
 *   otherFilingSheetExtender - Section 4's conditional description column
 *   section5SheetExtender   - Section 5's complete consigned-goods row
 *
 * The extender adds persisted Add Row Above/Add Row Below actions, keeps row
 * validation client-side until Next/Previous is pressed, and paints each
 * remaining invalid cell red while the row remains highlighted until complete.
 */
